{
  "name": "daily-discipline",
  "private": true,
  "version": "1.0.0",
  "description": "Daily Discipline tracker — web app + Capacitor iOS shell",
  "scripts": {
    "sync": "node scripts/copy-web.mjs && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
